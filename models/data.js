let tasks = []; // Array to store tasks
let users = []; // Array to store users
let taskId = 0; // Variable to simulate auto-incrementing task IDs
let userId = 0; // Variable to simulate auto-incrementing user IDs

module.exports = { tasks, users, taskId, userId };
